<p style="color: green"><%= notice %></p>

<h1><%= @video.title %></h1>

<p>
  <strong>説明:</strong><br>
  <%= simple_format(@video.body) %>
</p>

<p>
  <strong>YouTube動画:</strong><br>
  <iframe width="560" height="315"
          src="https://www.youtube.com/embed/<%= @video.youtube_url %>"
          frameborder="0"
          allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture"
          allowfullscreen>
  </iframe>
</p>

<div class="d-flex gap-2 mt-3">
  <%= link_to "投稿一覧に戻る", videos_path, class: "btn btn-secondary" %>
  <%= button_to "投稿を削除", @video, method: :delete, data: { confirm: "本当に削除しますか？" }, class: "btn btn-danger" %>
</div>
